<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme.
         Material3 parent keeps the window chrome neutral so that
         enableEdgeToEdge() + MuSyncTheme fully control system bar colours. -->
    <style name="Theme.MuSync" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/musync_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorPrimaryContainer">@color/musync_primary_variant</item>
        <item name="colorSecondary">@color/musync_secondary</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="android:windowBackground">@color/musync_background</item>
    </style>
</resources>
