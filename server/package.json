{
  "name": "musync-server",
  "version": "1.0.0",
  "description": "WebSocket signalling server for MuSync",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --runInBand",
    "lint": "eslint src __tests__"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "express": "^4.22.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "eslint": "^9.26.0",
    "jest": "^29.7.0",
    "socket.io-client": "^4.8.1",
    "supertest": "^7.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000
  }
}
